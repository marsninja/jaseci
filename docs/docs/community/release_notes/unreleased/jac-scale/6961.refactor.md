- **Internal: webhook tests reuse a single MongoDB container**: The webhook test suite now shares one MongoDB testcontainer across its MongoDB-backed cases, dropping the database between tests for isolation, instead of starting and stopping a fresh container per test (#6961).
