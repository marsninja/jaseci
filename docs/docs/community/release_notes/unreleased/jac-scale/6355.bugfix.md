- **Fix: Ephemeral port startup for sidecar servers**: `JacAPIServerCore.start` now pre-binds port 0 to avoid a TOCTOU race and writes the bound port via `write_server_ready_file`, enabling reliable `jac ai --tui` agent-server launch. (jaseci-labs/jaseci#6355)
