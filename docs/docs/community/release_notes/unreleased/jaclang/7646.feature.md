- **Native pathway completeness (#7626)**: the na backend closes its documented residual lowerings, deepens OSP, and matures the platform story. Entry-block assignments are now native module globals (CPython scoping parity -- lambdas and defs read them late-bound); capturing lambdas work inside generator frames; generic generators, generic methods, and generic-fns-as-values (concrete `Callable` bindings) monomorphize; `yield` inside `finally` and call-site unpacking (`f(*tuple)`, variadic `f(*list)`) lower. OSP gains multi-hop typed-edge chains (`[me ->:Follow:-> [?:Profile] --> [?:Tweet]]`), field-predicate filters (`[?:T, x > 1]`), and static operator-dunder dispatch on archetypes. Platform: scalar optionals (`int | None`) lower as tagged values instead of collapsing; narrow signed C returns sign-extend at the FFI seam; SysV float-struct passing, `DT_RUNPATH`, and float-repr CPython parity are pinned; PE data imports resolve through the IAT (`__imp_` indirection, loud errors for direct data refs); native modules can ship per-OS variants (`<name>.<os>.na.jac`, used by the new darwin dirent floor); `vendor-musl-<osarch>` cross-stages the static musl runtime and an aarch64 qemu CI lane runs the cross tests end-to-end.
