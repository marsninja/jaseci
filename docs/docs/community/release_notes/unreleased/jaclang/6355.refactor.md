- **Refactor: Centralize TTY detection for TUI backends**: `_first_tty_stream`, `_probe_tty`, and `_require_tty` in `tui_shared` now own the shared POSIX `/dev/pts` resolution used by both the in-process and subprocess `jac ai --tui` entrypoints. (jaseci-labs/jaseci#6355)
- **Refactor: Gate `test-binary.yml` on path filter**: The single-binary self-test workflow (which runs the full `jac/tests/` suite, including the in-process TUI tests under `jac/tests/cli/`) previously ran on every PR to `main` regardless of what changed. Added a `changes` path-filter job (mirroring the gate already used in `test-jaseci.yml`) that scopes `build` -- and its `test-compiler` / `test-runtime` dependents -- to PRs touching `jac/**`, the workflow itself, or `.github/actions/setup-jac/**`. Docs-only and out-of-tree plugin PRs no longer trigger a redundant binary build + full suite run. Pushes to `main`/`feat/**` and `workflow_dispatch` always run, so protected-branch coverage is unchanged. (jaseci-labs/jaseci#6355)
- **Refactor: de-stringly-type and de-duplicate ai_tui_na**: Removed the last stringly-typed option sets and duplicated constants flagged in review. Extracted one shared `idiv` into `width.na.jac` (collapsing the byte-identical `_idiv`/`_idiv_s` in editor/screen); split the ~95%-duplicated `tty/libc_tty.<linux|darwin>.na.jac` FFI backends into a shared `tty/libc_tty_base.na.jac` plus tiny per-platform `tty/tty_plat.<os>.na.jac` bindings+constants files (`build.sh`/`build_embed.sh` stage both); centralized the re-hardcoded RGB palette so `screen.na.jac`, `tool_block.na.jac`, and `markdown.na.jac` import from `theme.na.jac` (the pre-built SGR globals are reproduced byte-for-byte via `ansi_sgr`); and replaced `read_blob`'s `(str, bool)` tuple return with a named `BlobResult` object. No behavior change. (jaseci-labs/jaseci#6355)
- **Refactor: typed key model for the NA TUI**: `keys.na.jac` introduces a `KeyKind` enum and `Key` object so every dispatcher reasons about structured key kinds (`UP`, `CTRL`, `ENTER`, ...) instead of scattered `ord()` checks and duplicated `\033[A`/`\033OA` ladders. `parse_key` promotes a raw `tty_read_key` string into a `Key`; `handle_key`, `overlay_handle_key`, and `SelectList.handle_key` all accept `Key` instead of `str`. `test_pickers` gains 13 `parse_key` unit tests verifying the dual-encoding collapse. (jaseci-labs/jaseci#6355)
- **Refactor: unify TUI event loop behind a Transport seam**: Extract `poll → key → drain → render` into `tui_loop_once` with `TtyFd`, `EmbedPy`, and `ExternalDriver` adapters so `tui`, `host_embed`, and `host` no longer duplicate the loop inline. (jaseci-labs/jaseci#6355)
- **Refactor: centralize TUI stdio fd contract**: Name `RENDER_FD` and `IPC_FD`, return them from `tty_setup_stdio_contract`, store `render_fd` on `TuiState`, and stop threading bare `1`/`3` through `tui_core` call sites. (jaseci-labs/jaseci#6355)
- **Refactor: collapse scattered TUI global state into one TuiRuntime owner**: Each entry point previously owned its own `(TuiState, DiffEngine)` pair as module globals plus cross-cutting hidden globals (`_cmd_queue`, `_paint_buf`); `ipc_send_cmd(fd, cmd)` silently routed to the `ipc` module's `_cmd_queue` when `fd < 0`, so an IPC-parser module reached into queue state the embed/host loops then drained. `TuiRuntime { state, diff, transport, cmd_queue, paint_buf }` is now the single owner, passed down explicitly; the transport decision (live fd vs. buffered) reads `rt.state.ipc_fd` + `rt.cmd_queue` / `rt.paint_buf` as instance members. Two TUIs can coexist, and `ipc_send_cmd` is unit-testable with a fresh `CmdQueue`. The `host.na.jac` `:pub` C-ABI surface is unchanged. (jaseci-labs/jaseci#6355)
- **Refactor: replace TUI stringly-typed kinds with enums**: Convert the closed option sets typed as bare `str` into proper enums (`EventKind`, `TuiStatus`, `OverlayKind`, `AcKind`, `CmdKind`), mirroring the existing `KeyKind`/`TokenKind` pattern. The two parallel string-cascades over the same `Event.kind` set are now enum-keyed functions (`feed.na.jac::event_prefix`, `screen.na.jac::_kind_sgr`). `Event.kind` and `TuiState.status` arrive over IPC as free-form strings, so `ipc.na.jac` converts them once at the boundary via `parse_event_kind()`/`parse_tui_status()`; the rest of the TUI handles only typed enum values. (jaseci-labs/jaseci#6355)
- **Refactor: convert editor single-line edits to in-place list mutation**: Replace O(n) full-list rebuilds with O(1) in-place element assignment (`ed.lines[i] = ...`) in 7 editor functions (`editor_insert`, `editor_insert_text`, `editor_backspace`, `editor_forward_delete`, `editor_delete_to_line_end`, `editor_delete_to_line_start`, `editor_delete_word_back`), eliminating the per-keystroke rebuild that made single-line edits O(lines) and the overall editor O(lines²) over a typing session. (jaseci-labs/jaseci#6355)
