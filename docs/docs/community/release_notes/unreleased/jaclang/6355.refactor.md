- **Refactor: Centralize TTY detection for TUI backends**: `_first_tty_stream`, `_probe_tty`, and `_require_tty` in `tui_shared` now own the shared POSIX `/dev/pts` resolution used by both the in-process and subprocess `jac ai --tui` entrypoints. (jaseci-labs/jaseci#6355)
- **Refactor: Gate `test-binary.yml` on path filter**: The single-binary self-test workflow (which runs the full `jac/tests/` suite, including the in-process TUI tests under `jac/tests/cli/`) previously ran on every PR to `main` regardless of what changed. Added a `changes` path-filter job (mirroring the gate already used in `test-jaseci.yml`) that scopes `build` -- and its `test-compiler` / `test-runtime` dependents -- to PRs touching `jac/**`, the workflow itself, or `.github/actions/setup-jac/**`. Docs-only and out-of-tree plugin PRs no longer trigger a redundant binary build + full suite run. Pushes to `main`/`feat/**` and `workflow_dispatch` always run, so protected-branch coverage is unchanged. (jaseci-labs/jaseci#6355)
- **Refactor: de-stringly-type and de-duplicate ai_tui_na**: Removed the last stringly-typed option sets and duplicated constants flagged in review. Extracted one shared `idiv` into `width.na.jac` (collapsing the byte-identical `_idiv`/`_idiv_s` in editor/screen); split the ~95%-duplicated `tty/libc_tty.<linux|darwin>.na.jac` FFI backends into a shared `tty/libc_tty_base.na.jac` plus tiny per-platform `tty/tty_plat.<os>.na.jac` bindings+constants files (`build.sh`/`build_embed.sh` stage both); centralized the re-hardcoded RGB palette so `screen.na.jac`, `tool_block.na.jac`, and `markdown.na.jac` import from `theme.na.jac` (the pre-built SGR globals are reproduced byte-for-byte via `ansi_sgr`); and replaced `read_blob`'s `(str, bool)` tuple return with a named `BlobResult` object. No behavior change. (jaseci-labs/jaseci#6355)
