- **Refactor: single source of truth for the pinned LLVM slices and glibc floor**: the per-platform LLVM slice pins (dirname / triple / manifest sha / zip size) move from duplicated tables in `payload.zig` and `build.zig` into `jac/launcher/llvm_release.zig`, imported by both. The Linux shim link path (zig c++/libc++ vs host g++/libstdc++) is now chosen by the slice's C++ runtime (`*-libcxx` triple suffix) instead of a hardcoded arch check, and the release workflow derives its glibc-floor assertion from the matrix `zig_target` instead of a second hardcoded `2.17` — the aarch64 leg's launcher is now floor-checked too. No change to shipped binaries; this makes the pending aarch64 drop to glibc 2.17 a constants-only edit.
