- **Feature: `jac ai --tui` flag and TUI session plumbing**: Adds `--tui` to `jac ai` (mutually exclusive with `--ui`), `AgentRequest.tui_active` to suppress duplicate server-side terminal output during Ink sessions, `write_server_ready_file` for ephemeral-port handoff, and runtime fixes so binding to port 0 records the OS-assigned port correctly. (jaseci-labs/jaseci#6355)
