- **Refactor: shadcn tooling and skill docs go markerless**: now that codespace placement is inferred (JSX and npm imports mark code client), `jac install --shadcn` and `jac create --use jac-shadcn` scaffold plain `.jac` components instead of `.cl.jac` (54 registry files renamed), and the generated `lib/utils.jac` / `main.jac` drop the `cl` marker. The bundled agent skill docs were updated to teach the inference model; `sv import`, `sv` pins, and `.sv.jac` service modules are preserved because server boundaries are not inferred. Legacy `.cl.jac` files in existing projects are still recognized by `jac install`/`jac retheme`.
