- **Fix: `Union[('A', 'B')]` with string type names no longer produces false type errors**: String names inside `Union[(...)]` were treated as plain text instead of the actual types they refer to, so valid return values and assignments were incorrectly rejected by the type checker. The type checker now correctly resolves these strings to their types.
