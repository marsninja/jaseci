- **Breaking: `view` is now a reserved keyword**: `view` introduces the new view declarator and can no longer be used as an ordinary identifier (variable, parameter, function, walker, etc.). Existing code that uses the name `view` must escape it with a backtick (`` `view ``), which keeps the original name.
