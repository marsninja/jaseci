- **Native self-hosting stage 2: the six census-ranked compiler-tree blockers are cleared (#7626 C7)**: `type_registry.jac` and `llvm/__init__.jac` now lower natively, `wasm_linker.jac`/`macho_linker.jac` check clean, and `ts_decl_parser.jac` clears its recorded blocker. Checker `Any`/`Unknown` cascades no longer hard-error downstream uses (variadic tuple extraction, binary ops, subscripts, augmented assignment); native dict helper families key pointer values by pointee identity and `dict.get` retains its result instead of returning a borrowed pointer that scope-exit frees; `os.environ` `.get`/`[]=`/`.pop` lower via the getenv/setenv intrinsics; nested sibling closures prototype together with a fixpoint over transitive capture sets (forward and mutual sibling calls work, capture misses diagnose instead of crashing the IR builder); generic class templates are excluded from direct lowering while explicit-`self` methods finally lower with correct receiver mapping; and `bytearray` lowers as a `List.u8` container with a dedicated native runtime. Cross-module method calls trust an imported module's recorded native layout -- resolved through the owning program even after closure release slims the module -- and only fall back to conservative per-method demotion for jaclang-internal modules whose layout is unreachable, so sealed-binary builds neither bind dead native bodies (runtime aborts) nor demote live cross-module calls.
