- **Refactor: Impl/test annexes merged into the unified module tree**: `.impl.jac` and `.test.jac` annex bodies are now spliced into their head module's own tree by a single pass, instead of hanging off `Module.impl_mod`/`test_mod`/`variant_mod` side-lists that the generic walk, sub-node table, and JIR serializer could not see. Every compiler walk, the JIR cache, and tooling now cover annex content directly; the side-list fields and the recompile-on-cache-hit reattachment hack are removed.
- **Fix: Duplicate diagnostics inside matched `impl` bodies**: A type error in a separated `impl` body (e.g. `def f; impl f { ... }`) was reported twice. It is now reported once.
- **Fix: Annex semantic tokens in the language server**: Hovering, go-to-definition, and semantic highlighting inside an `.impl.jac`/`.test.jac` file now resolve again, served per source file by provenance from the merged tree.
