- **Refactor: Migrate stale backtick-`root` triggers in test fixtures**: Ten test fixtures still used the removed `` `root `` walker ability trigger (dropped in #4496, now rejected as E1116). They only survived because the `jac check` gate ignores the `tests` tree and each test asserts on a different diagnostic code. All are updated to the canonical `with Root entry`; the intentional `BadBacktickRoot` negative test that asserts E1116 is left unchanged.
