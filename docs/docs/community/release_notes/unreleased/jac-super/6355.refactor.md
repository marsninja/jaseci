- **Refactor: Centralize TTY detection for TUI backends**: `_first_tty_stream`, `_probe_tty`, and `_require_tty` in `tui_shared` now own the shared POSIX `/dev/pts` resolution and Windows `isatty()` probe used by both the in-process and subprocess `jac ai --tui` entrypoints. (jaseci-labs/jaseci#6355)
