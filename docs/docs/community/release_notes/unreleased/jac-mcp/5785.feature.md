- **Feat: Add jac-write authoring skill**: New self-contained skill at `jac-mcp/jac_mcp/skills/jac-write/` with `SKILL.md`, `pitfalls.md`, `paradigms.md`, and validated `.jac` examples. Teaches models to write idiomatic Jac via a Python-vs-Jac reset, a mandatory compile-before-present workflow using the `jac` CLI, 35 WRONG-vs-RIGHT pitfall pairs, paradigm playbooks for OSP/MTP/codespaces, and the canonical multi-file full-stack shape. Ships in the `jac-mcp` package wheel (pyproject.toml package-data updated); copy the directory into `~/.claude/skills/jac-write/` for use without an MCP server.
