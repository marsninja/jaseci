"""Output emitters for `jac eject`: each module writes one half of the
`ejected/` output (backend.jac emits the FastAPI backend that runs walkers on
the jaclang runtime; the frontend half is emitted by the eject orchestrator)."""
