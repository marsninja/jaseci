import from datetime { datetime }
import from uuid { UUID, uuid4 }
import from jaclang.pycore.ast.constant { EdgeDir }
import from jaclang.plugin {
    Archetype,
    ArchetypeProtocol,
    DSFunc,
    AbsRootHook,
    hookimpl
}
import math;
import numpy as np;
